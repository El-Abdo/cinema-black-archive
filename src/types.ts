export type Director = {
    id: number;
    name: string;
    bio: string;
    portrait_url: string;
  };
  
  export type Film = {
    id: number;
    title: string;
    release_year: number;
    poster_url: string;
  };
  